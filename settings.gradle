def owAlarmsPath = './openwhisk-alarms'
def owAlarmsDirectory = new File(owAlarmsPath)

include 'alarmtests'; project(':alarmtests').projectDir = new File(owAlarmsDirectory, 'tests')
include 'tests'

rootProject.name = 'package-alarms'

gradle.ext.openwhisk = [
        version: '1.0.0-SNAPSHOT'
]

gradle.ext.scala = [
    version: '2.12.7',
    compileFlags: ['-feature', '-unchecked', '-deprecation', '-Xfatal-warnings', '-Ywarn-unused-import']
]
